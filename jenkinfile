pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'building an app'
            }
        }
       stage('test') {
            steps {
                echo 'running test'
            }
       }
        stage('deploye') {
            steps {
                echo 'deploying the app'
            }
        }
     }
}
